# SFX Savdo Bot - Система закупок

Бот для управления закупками с ролями: Покупатель, Продавец, Человек на складе и Администратор.

## 🏗️ Структура проекта

```
SFX_SAVDO/
├── bot.py              # Основной файл бота
├── database.py         # Работа с базой данных
├── config.py           # Конфигурация
├── requirements.txt    # Зависимости
├── .env               # Переменные окружения
└── README.md          # Документация
```

## 📋 Требования

- Python 3.8+
- PostgreSQL
- Telegram Bot Token

## 🚀 Установка и настройка

### 1. Создание виртуального окружения

```bash
python -m venv venv_new_new
source venv_new_new/bin/activate
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка базы данных PostgreSQL

#### Создание базы данных:

```sql
-- Подключитесь к PostgreSQL
psql -U postgres

-- Создайте базу данных
CREATE DATABASE sfx_savdo_db;

-- Создайте пользователя (опционально)
CREATE USER sfx_user WITH PASSWORD 'your_password';

-- Дайте права пользователю
GRANT ALL PRIVILEGES ON DATABASE sfx_savdo_db TO sfx_user;

-- Выйдите из psql
\q
```

### 4. Настройка переменных окружения

Создайте файл `.env`:

```env
# Telegram Bot Token
BOT_TOKEN=your_bot_token_here

# PostgreSQL настройки
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sfx_savdo_db
DB_USER=postgres
DB_PASSWORD=your_password_here

# Администраторы (ID пользователей Telegram через запятую)
ADMIN_IDS=123456789,987654321
```

### 5. Получение Telegram Bot Token

1. Найдите @BotFather в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в файл `.env`

### 6. Получение ID администраторов

1. Добавьте бота в чат
2. Отправьте боту команду `/start`
3. Найдите свой ID через @userinfobot
4. Добавьте ID в переменную `ADMIN_IDS` в файле `.env`

## 🏃‍♂️ Запуск бота

### 1. Активация виртуального окружения

```bash
source venv_new_new/bin/activate
```

### 2. Запуск бота

```bash
python bot.py
```

## 👥 Система ролей

### 🔧 Администратор
- **Как добавить:** Укажите ID в переменной `ADMIN_IDS` в файле `.env`
- **Права:**
  - Одобрение/отклонение пользователей
  - Управление системой
  - Просмотр статистики

### 👤 Покупатель
- **Как добавить:** 
  1. Пользователь отправляет `/register`
  2. Выбирает роль "Покупатель"
  3. Администратор одобряет через `/approve <telegram_id>`
- **Функции:**
  - Создание заявок на покупку
  - Получение предложений от продавцов
  - Отслеживание заказов

### 🏪 Продавец
- **Как добавить:**
  1. Пользователь отправляет `/register`
  2. Выбирает роль "Продавец"
  3. Автоматически одобряется
- **Функции:**
  - Просмотр активных заявок
  - Отправка предложений
  - Отслеживание своих предложений

### 🏭 Человек на складе
- **Как добавить:**
  1. Пользователь отправляет `/register`
  2. Выбирает роль "Человек на складе"
  3. Администратор одобряет через `/approve <telegram_id>`
- **Функции:**
  - Прием товаров от продавцов
  - Подтверждение получения товаров
  - Уведомление покупателей

## 📊 Структура базы данных

### Таблица `users`
- `id` - Уникальный идентификатор
- `telegram_id` - ID пользователя в Telegram
- `username` - Имя пользователя
- `first_name` - Имя
- `last_name` - Фамилия
- `phone` - Номер телефона
- `role` - Роль (buyer/seller/warehouse/admin)
- `is_approved` - Одобрен ли пользователь
- `created_at` - Дата создания
- `updated_at` - Дата обновления

### Таблица `purchase_requests`
- `id` - Уникальный идентификатор
- `buyer_id` - ID покупателя
- `supplier_name` - Название поставщика
- `object_name` - Название объекта
- `product_name` - Название товара
- `quantity` - Количество
- `unit` - Единица измерения
- `material_description` - Описание материала
- `status` - Статус заявки
- `created_at` - Дата создания

### Таблица `seller_offers`
- `id` - Уникальный идентификатор
- `request_id` - ID заявки
- `seller_id` - ID продавца
- `price` - Цена
- `total_amount` - Общая сумма
- `status` - Статус предложения
- `created_at` - Дата создания

### Таблица `deliveries`
- `id` - Уникальный идентификатор
- `offer_id` - ID предложения
- `warehouse_user_id` - ID человека на складе
- `buyer_id` - ID покупателя
- `status` - Статус доставки
- `received_at` - Дата получения
- `created_at` - Дата создания

## 🔧 Административные команды

### Одобрение пользователя
```
/approve <telegram_id>
```

### Отклонение пользователя
```
/reject <telegram_id>
```

### Панель администратора
```
/admin
```

## 📝 Процесс работы

### 1. Регистрация пользователей
- Покупатели и люди на складе требуют одобрения администратора
- Продавцы автоматически одобряются

### 2. Создание заявок
- Покупатель создает заявку с указанием требований
- Заявка автоматически отправляется всем продавцам

### 3. Предложения продавцов
- Продавцы видят активные заявки
- Могут отправлять предложения с ценами

### 4. Одобрение предложений
- Покупатель выбирает предложение
- Создается запись о доставке

### 5. Доставка и прием
- Продавец доставляет товар на склад
- Человек на складе подтверждает получение
- Покупатель получает уведомление

## 🛠️ Устранение неполадок

### Ошибка подключения к базе данных
1. Проверьте настройки в файле `.env`
2. Убедитесь, что PostgreSQL запущен
3. Проверьте права доступа пользователя

### Бот не отвечает
1. Проверьте токен бота в файле `.env`
2. Убедитесь, что бот не заблокирован
3. Проверьте логи на наличие ошибок

### Пользователи не получают уведомления
1. Убедитесь, что пользователи начали диалог с ботом
2. Проверьте ID пользователей в базе данных
3. Проверьте права бота на отправку сообщений

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи бота
2. Убедитесь в правильности настроек
3. Проверьте подключение к базе данных
4. Убедитесь в корректности Telegram Bot Token 