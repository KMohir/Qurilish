# SFX SAVDO - Система закупок Telegram бот

## Описание

Telegram бот для автоматизации процесса закупок с ролями:
- **Покупатель** - создает заявки на покупку
- **Продавец** - отправляет предложения по заявкам
- **Человек на складе** - подтверждает получение товаров
- **Администратор** - управляет пользователями

## Функциональность

### Для покупателей:
- Создание заявок в Excel или текстовом формате
- Просмотр своих заявок и предложений
- Получение уведомлений о доставке

### Для продавцов:
- Получение заявок от покупателей
- Отправка предложений в Excel или текстовом формате
- Автоматическая регистрация (без одобрения)

### Для людей на складе:
- Просмотр ожидающих доставок
- Подтверждение получения товаров
- Уведомление покупателей о получении

### Для администраторов:
- Одобрение регистрации покупателей и людей на складе
- Просмотр статистики

## Установка и настройка

### 1. Требования

- Python 3.10.18
- PostgreSQL 12+
- pip

### 2. Клонирование и установка зависимостей

```bash
# Клонируйте проект
git clone <repository_url>
cd SFX_SAVDO

# Создайте виртуальное окружение
python3.10 -m venv venv
source venv/bin/activate  # На Linux/Mac
# или
venv\Scripts\activate  # На Windows

# Установите зависимости
pip install -r requirements.txt
```

### 3. Настройка базы данных PostgreSQL

#### 3.1 Установка PostgreSQL

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install postgresql postgresql-contrib
```

**CentOS/RHEL:**
```bash
sudo yum install postgresql postgresql-server
sudo postgresql-setup initdb
sudo systemctl start postgresql
```

**Windows:**
Скачайте и установите с официального сайта: https://www.postgresql.org/download/windows/

#### 3.2 Создание базы данных

```bash
# Подключитесь к PostgreSQL
sudo -u postgres psql

# Создайте базу данных и пользователя
CREATE DATABASE sfx_savdo_db;
CREATE USER sfx_user WITH PASSWORD 'your_secure_password';
GRANT ALL PRIVILEGES ON DATABASE sfx_savdo_db TO sfx_user;
\q
```

#### 3.3 Инициализация таблиц

```bash
# Запустите SQL скрипт
psql -U sfx_user -d sfx_savdo_db -f database.sql
```

### 4. Настройка переменных окружения

Создайте файл `.env` на основе `env_example.txt`:

```bash
cp env_example.txt .env
```

Отредактируйте `.env` файл:

```env
# Токен бота Telegram (получите у @BotFather)
BOT_TOKEN=your_actual_bot_token

# Настройки базы данных
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sfx_savdo_db
DB_USER=sfx_user
DB_PASSWORD=your_secure_password
```

### 5. Получение токена бота

1. Найдите @BotFather в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `.env` файл

### 6. Запуск бота

```bash
# Активируйте виртуальное окружение (если еще не активировано)
source venv/bin/activate

# Запустите бота
python bot.py
```

## Структура базы данных

### Таблица `users`
- `id` - уникальный идентификатор
- `telegram_id` - ID пользователя в Telegram
- `username` - имя пользователя в Telegram
- `full_name` - полное имя
- `phone_number` - номер телефона
- `role` - роль (buyer/seller/warehouse/admin)
- `is_approved` - статус одобрения
- `created_at` - дата создания

### Таблица `purchase_requests`
- `id` - уникальный идентификатор заявки
- `buyer_id` - ID покупателя
- `supplier` - поставщик
- `object_name` - название объекта
- `product_name` - название товара
- `quantity` - количество
- `unit` - единица измерения
- `material_description` - описание материала
- `request_type` - тип заявки (excel/text)
- `status` - статус заявки
- `created_at` - дата создания

### Таблица `seller_offers`
- `id` - уникальный идентификатор предложения
- `purchase_request_id` - ID заявки
- `seller_id` - ID продавца
- `price` - цена
- `total_amount` - общая сумма
- `offer_type` - тип предложения (excel/text)
- `status` - статус предложения
- `created_at` - дата создания

### Таблица `deliveries`
- `id` - уникальный идентификатор доставки
- `offer_id` - ID предложения
- `warehouse_user_id` - ID человека на складе
- `delivery_status` - статус доставки
- `received_at` - дата получения
- `completed_at` - дата завершения
- `created_at` - дата создания

## Использование

### Регистрация пользователей

1. **Продавцы** - регистрируются автоматически без одобрения
2. **Покупатели и люди на складе** - требуют одобрения администратора

### Создание заявки

1. Покупатель нажимает "📋 Создать заявку"
2. Выбирает формат (Excel или текст)
3. Заполняет данные
4. Заявка автоматически отправляется всем продавцам

### Отправка предложения

1. Продавец получает уведомление о новой заявке
2. Отправляет предложение в Excel или текстовом формате
3. Покупатель получает предложение для рассмотрения

### Подтверждение доставки

1. Человек на складе видит ожидающие доставки
2. Нажимает "✅ Получено" при получении товара
3. Покупатель получает уведомление о получении

## Команды администратора

- `/start` - запуск бота
- Просмотр пользователей на одобрении
- Одобрение/отклонение регистраций
- Просмотр статистики

## Безопасность

- Все пароли хранятся в переменных окружения
- Валидация номеров телефонов
- Проверка ролей пользователей
- Логирование всех действий

## Поддержка

При возникновении проблем:

1. Проверьте логи в консоли
2. Убедитесь в правильности настроек базы данных
3. Проверьте токен бота
4. Убедитесь в установке всех зависимостей

## Лицензия

Проект разработан для внутреннего использования компании SFX. 